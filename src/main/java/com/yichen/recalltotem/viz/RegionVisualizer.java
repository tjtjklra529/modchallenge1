package com.yichen.recalltotem.viz;

import com.yichen.recalltotem.config.ProtectedRegion;
import net.minecraft.server.network.ServerPlayerEntity;

public final class RegionVisualizer {
    private RegionVisualizer() {}

    public static void previewRegion(ServerPlayerEntity player, ProtectedRegion region) {
        // placeholder: spawn particles or send packets to show region boundary
    }
}
